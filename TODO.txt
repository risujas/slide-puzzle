
Allow the user to manually drag the tiles instead of just clicking to move them

Add sound effects for moving pieces, background music, victory message, etc

Allow moving the empty slot with keyboard 